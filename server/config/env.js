import'dotenv/config';const req=['APP_ORIGIN','DB_HOST','DB_NAME','DB_USER','DB_PASSWORD','SESSION_SECRET','RAZORPAY_KEY_ID','RAZORPAY_KEY_SECRET','RAZORPAY_WEBHOOK_SECRET','TURNSTILE_SECRET_KEY'];for(const k of req)if(process.env.NODE_ENV==='production'&&!process.env[k])throw Error(`Missing required server environment variable: ${k}`);for(const k of['PORT','DB_PORT'])if(process.env[k]&&!Number.isInteger(Number(process.env[k])))throw Error(`Invalid numeric environment variable: ${k}`);export const env={production:process.env.NODE_ENV==='production',port:Number(process.env.PORT||3000)};
