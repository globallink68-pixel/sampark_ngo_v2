import{describe,it,expect}from'vitest';import{canAdmin,contactOk,transition,turnstileOk}from'../src/lib/rules.js';
describe('final mocked security paths',()=>{it('denies unauthenticated admin',()=>expect(canAdmin(null)).toBe(false));it('denies regular member admin actions',()=>expect(canAdmin('member')).toBe(false));it('allows an admin action',()=>expect(canAdmin('admin')).toBe(true));it('logout-equivalent absent role denies access',()=>expect(canAdmin(undefined)).toBe(false));it('requires contact token in production',()=>expect(turnstileOk(true,'')).toBe(false));it('accepts valid contact token',()=>expect(turnstileOk(true,'token')).toBe(true));it('rejects honeypot contact',()=>expect(contactOk({name:'A',email:'a@b.co',subject:'s',message:'m',website:'x'})).toBe(false));it('models invalid payment signature as no state transition',()=>expect(transition('created','created')).toBe('created'));it('models duplicate payment verification idempotently',()=>expect(transition('paid','paid')).toBe('paid'));it('prevents paid webhook downgrade',()=>expect(transition('paid','failed')).toBe('paid'));it('allows failed webhook only before paid',()=>expect(transition('created','failed')).toBe('failed'))});
