export const contactOk=d=>!!d&&!d.website&&!!d.name?.trim()&&/^\S+@\S+\.\S+$/.test(d.email||'')&&!!d.subject?.trim()&&!!d.message?.trim();export const transition=(old,next)=>old==='paid'?'paid':next==='paid'?'paid':next==='failed'?'failed':old;export const canAdmin=role=>role==='admin';export const contactPayload=d=>contactOk(d)?d:throwError();const throwError=()=>{throw Error('Invalid contact details')};
